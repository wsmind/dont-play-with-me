--[[ 

]]

require("math.vec2")

Config = {
	heroHorizontalSpeed = vec2(5,0), -- How much the hero moves when it's walking to the right
	heroVerticalSpeed = vec2(0,-5), -- How much the hero moves when it's jumping upwards
	gravity = vec2(0,9) -- Strength of the gravity
}